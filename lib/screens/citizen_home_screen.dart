import 'package:flutter/material.dart';

class CitizenHomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Citizen Home'),
      ),
      body: Center(
        child: Text('Welcome to the Citizen Home Screen!'),
      ),
    );
  }
}
